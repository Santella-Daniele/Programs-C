#include <stdio.h>
#include <stdlib.h>
#include<time.h>
int funzione_di_Dio(int size);
int simulazione(int occorrenze[], int size);
void stampa_risultati(int occorrenze[], int size);

int main() {
	funzione_di_Dio(7);	
}


	int funzione_di_Dio(int size){;
	int occorrenze[7]={ 0 };
	srand(time(NULL));
	int simulazione(int occorrenze[], int size) {
	int i;

		for(i=0;i<10;i++){
			occorrenze[1+rand()% 6]++;
	 }
	}
	void stampa_risultati(int occorrenze[], int size) {
	int i;
		for(i=1;i<size;i++){
			printf(" Il numero  %d e'  uscito %d volta/e\n",i,occorrenze[i]);
      }
    }	
  }


			
